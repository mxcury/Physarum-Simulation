#ifndef SPAWN_SETTINGS_H
#define SPAWN_SETTINGS_H

enum class SpawnPosition {
    RANDOM,
    CIRCLE,
    RADIUS,
    CENTRE
};

enum class InitialHeading {
    RANDOM,
    CENTRED,
    EDGE
};

#endif
